# 取片个人敏感配置模板。复制成 ~/.baokuan-factory/secrets.env（在仓库外、不进 git、不共享）。
#
# 抖音作者枚举(take.py douyin)要登录态 cookie。两种拿法：
#   a) 浏览器登录抖音 → F12 Network 里随便点一条 douyin.com 请求 → 复制 Request Headers 的 Cookie 整行,粘到下面;
#   b) 留空,`pip install browser_cookie3`,脚本自动从 Safari/Chrome 取(需在浏览器登录抖音)。
DOUYIN_COOKIE=
