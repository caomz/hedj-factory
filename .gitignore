# personal profiles distilled by 女娲 — each teammate's own, not for the shared repo
skills/0xkaiwen/
skills/*-profile/
# installer backups
.bak-*
# editing project junk
node_modules/
*.wav
*.mp4
.DS_Store

# optimization run artifacts (keep harness + eval set + desc, drop transient logs/results)
.optimize/*.log
.optimize/faithful*.json
.optimize/round*.json
.optimize/round*.log
.optimize/mini.*
.optimize/SKILL.md.orig
