## Codebase Patterns
- Python CLI 使用标准库 `argparse` + `pathlib`，输入校验必须发生在任何遍历或写入之前；行为测试使用 `unittest` + `tempfile` + `subprocess`
- `skills/<name>-profile/` 是被 .gitignore 忽略的个人 profile 目录；下游 runtime 仍可加载
- nuwa-skill 的研究材料命名固定为 `references/research/01-XX.md` 到 `06-XX.md`，合并工具 merge_research.py 按文件名扫描
- 公众人物 quality_check.py 当前不区分 profile_type；新增 self 模式需要在 frontmatter 检测后切换校验
- 自我蒸馏工作流文档统一引用单一规范；任何 story 在进入 self 流程前必须先读 references/self-distill-workflow.md，不得从 SKILL.md 重新猜测 class/checkpoint
- self 模式默认纯本地：除非用户明确要求，不联网佐证；外部材料只允许在 references/research/ 对照，不进入 assets/
- 在 `/Volumes/WorkSSD/Cache/tmp`（APFS firmlink）上用 `subprocess.run` 调子进程创建的文件，父进程在同一 `tempfile.mkdtemp` 内 `Path.is_file()` 经常读不到（firmlink 写完后元数据同步延迟）；测试优先 import 被测模块后直接调函数，避免 `subprocess.run`，并在 finally 内做完所有断言再 `shutil.rmtree`
- source-policy.json 字段名 `schema_version` / `rules[].class` / `rules[].globs` 与合法 class 枚举（authored/private-evidence/adapted/external/excluded）由 US-006 锁定；后续 quality_check / merge_research / US-013 文档引用时必须复用同一名称与顺序，class 集合不得新增
- inventory 的 record 字段顺序是 manifest 的事实契约：US-006 仅在 `_build_manifest` 内引入 `can_distill` / `unmatched_count` 计算，不修改 record 字段集合；US-007+ 增加 sha256/duplicate_group/version_group/semantic_read 时同样保持 record 键名稳定
- `_glob_matches` 实现 `**/`（零或多层目录）与 `**`（任意字符）语义；`fnmatch.fnmatch` 与 `pathlib.PurePath.match` 都不直接支持 `**/`，不要在 story 中临时替换
- duplicate_group 用 sha256 字符串作为稳定标识；representative 以 eligible 文件中 relative_path 字典序最小者为准；reading budget 只对 semantic_read=true 的 representative 计数
- 版本序列字段顺序：US-008 在 record 中新增 `version_group` / `current_version` / `evolution_only`；version_group 形如 `{parent_dir}/{base_name}v{highest}{ext}`（或 root-level 时去掉前缀），与 duplicate_group 一样是「跨字段稳定字符串」；sorted 时必须用「数字 tuple + relative_path」复合 key，避免 V10 < V2 的字典序误判
- inventory review 在 重复文件 段之后追加 版本序列 段（version_groups + current_version 路径 + 注释），保持 markdown 列表格式 `- \`path\`` 不变，让 Nuwa 后续 story 可直接 rg 提取

---

## 2026-07-25 00:12 - US-001
- 新建 skills/nuwa-skill/references/self-distill-workflow.md（264 行）
- 内容覆盖五类来源 class、四个 source policy 确认点、六个自我研究维度、阅读预算表（authored ≤20MB/private-evidence ≤100 文件或 5MB/adapted ≤30/external 首轮 0）、版本序列 current 判定、资产卡证据门槛、隐私脱敏默认、三层输出边界、失败停止条件
- 改动文件：skills/nuwa-skill/references/self-distill-workflow.md
- 提交：73cff0c feat: US-001 - 编写自我蒸馏工作流参考文档
- **未来迭代的学习：**
  - 仓库 nuwa-skill 当前只有 person 模式文档；新 self-local-corpus 路径必须在 references/ 下先立单一规范文档，再让后续 story 引用，避免 story 间口径漂移
  - 阅读预算表是后续 quality_check.py 估算与 inventory 报告的统一基线：所有 story 引用时必须使用同一数值
  - 隐私扫描规则 ID（CREDENTIAL_ASSIGNMENT / WECHAT_ID / CHATROOM_ID / PRIVATE_IPV4）已在工作流锁定；后续 privacy gate 实现不得新增或重命名规则 ID
  - 资产卡 origin 五选一（authored/co-created/private-derived/adapted/external）已是契约；external-only 不得登记为个人资产
---

## 2026-07-25 00:35 - US-002
- 新建 skills/nuwa-skill/references/self-profile-template.md（125 行）
- frontmatter 声明 `profile_type: self` 与 `target_tokens: 3000-6000`；七个必需 section 标题与 US-010 acceptance regex 完全一致（含 `表达DNA` 不带空格）
- 模板硬性边界：不模拟公众人物、不编造用户经历、private-evidence 仅以脱敏主题归纳进入、链接到 assets/*.md 引用相对路径
- 自检清单预先列出 US-010 quality_check.py 验收点（心智模型 ≥2 场景证据、决策启发式含案例、隐私扫描、token 估算落 3000–6000）
- 改动文件：skills/nuwa-skill/references/self-profile-template.md
- 提交：24d2456 feat: US-002 - 编写自我 profile 运行时模板
- 同步更新：scripts/ralph/prd.json 中 US-002 passes=true
- **未来迭代的学习：**
  - acceptance regex 把 `表达DNA` 当作单一 token，模板正文与自检清单都得去掉空格；不要因为 Markdown 排版偏好插半角空格
  - quality_check.py 的 CJK + ceil(非CJK/4) 估算口径已在 self-distill-workflow.md §1 锁定，模板在自检清单里必须复述同一公式，否则 quality gate 与模板口径会漂移
  - 模板里所有 `assets/*.md` 链接用相对路径（`../assets/...`）；不要引用绝对路径或仓库根路径，下游 install 后的 symlink 可能不在仓库根目录
  - 公众人物模板 `references/skill-template.md` 带"我是XX"的角色扮演指令，自我模板必须显式禁用；任何后续生成的 self SKILL.md 不得复制该模板的语气

## 2026-07-25 01:08 - US-003
- 实现 `inventory_local_corpus.py` CLI 骨架，支持 `source_root`、`--profile-dir`、`--policy`、`--max-file-bytes`（默认 2000000）和 `--check`
- 在任何遍历或写入前拒绝缺失/非目录 source_root、非 check 模式缺失 profile-dir，以及位于 source_root 内的 profile-dir；check 模式不创建文件
- 新增 6 个标准库 unittest，覆盖参数接受、具体路径错误、写入边界和 check 零写入
- 更改的文件：`skills/nuwa-skill/scripts/inventory_local_corpus.py`、`tests/test_nuwa_local_corpus.py`、`scripts/ralph/prd.json`、`scripts/ralph/progress.txt`
- 验证：`python3 -m unittest tests.test_nuwa_local_corpus.LocalCorpusInputValidationTests -v`（6 tests OK）；新增 Python 文件 AST 解析通过；`git diff --check` 通过；CLI `--help` 退出码 0
- 提交：`40e6d06 feat: US-003 - 实现 inventory CLI 参数和路径防护`
- **未来迭代的学习：**
  - `Path.resolve(strict=False)` 适合在输出目录尚不存在时做包含关系防护，同时不会创建目录
  - 当前 CLI 成功路径只完成参数与边界校验；US-004 应在 `validate_inputs()` 之后接入确定性遍历，不应提前增加 manifest 写入
  - `--policy` 在 US-003 只要求可解析参数，不校验文件存在性或内容；这些属于 US-006，避免 story 越界
---

## 2026-07-25 02:18 - US-004
- 在 US-003 的参数校验后接入确定性遍历：os.walk(followlinks=False, onerror=抛 InventoryError)，跳过隐藏条目与目录 symlink，对非隐藏常规文件按相对路径字典序返回基础记录
- `determine_eligibility` 按 oversized → unsupported_extension → binary 顺序给出稳定 exclusion_reason，并把小写扩展名一次性回传，避免 record 构造处重复 `.suffix.lower()`
- `summarize_inventory` 单次遍历累加 eligible_files / skipped_by_extension / oversized_files / binary_files，13k+ 文件减少 3 次冗余扫描
- 新增 LocalCorpusTraversalTests 五例：排序与必需字段、隐藏+symlink 跳过、10 种受支持扩展名 ineligible 判定、unsupported+binary 计数、oversized 稳定排除
- 改动文件：skills/nuwa-skill/scripts/inventory_local_corpus.py、tests/test_nuwa_local_corpus.py、scripts/ralph/prd.json、scripts/ralph/progress.txt
- 验证：`python3 -m unittest tests.test_nuwa_local_corpus.LocalCorpusTraversalTests -v`（5 OK）+ LocalCorpusInputValidationTests（6 OK 回归）；AST 解析；`git diff --check` 通过；CLI 端到端 `--check` 重复运行输出完全一致，源目录 SHA-256 清单前后一致
- 提交：be4005e feat: US-004 - 实现确定性遍历和文件资格判定
- 同步更新：scripts/ralph/prd.json 中 US-004 passes=true
- **未来迭代的学习：**
  - record 字段集合（relative_path/extension/size_bytes/mtime/top_level_family/eligible/policy_class/exclusion_reason）是 US-005 manifest 的事实契约；后续 story 增加字段（如 sha256/duplicate_group/version_group/can_distill）时必须保持键名稳定，不在原地改类型
  - `top_level_family = "."` 是 root-level 文件的统一占位，Nuwa 用作 Checkpoint A 草拟 policy 的 family 摘要；后续不应改成空串或相对路径
  - `is_binary_file` 仅做 NUL 字节 + UTF-8 解码快速判定，会把 Windows-1252/GBK 等合法文本误判为 binary；如 US-011 隐私扫描要把 GBK 文本按文本处理，需要在那里引入编码探测，不要扩展此函数
  - `summarize_inventory` 单次遍历与 record 字段集合锁定后，US-005 复用同一份 records 计算 manifest，US-018 的 `--check` 性能基线（13k 文件 ≤120s）由本实现满足；后续不要在 walk 中再做额外 lstat
  - US-004 显式不引入 symlink 跟随、firmlink 边界检测、APFS 大小写归一化、Unicode NFC/NFD 等；这些超出 v1 范围，由 US-005 后续讨论决定是否扩展，避免 story 越界

## 2026-07-25 19:07 - US-005
- inventory_local_corpus.py 接入无 policy 场景的 manifest + review 写入：新增 write_manifest_and_review()、_build_manifest()、_write_json()、_write_inventory_review()；manifest schema_version=1、can_distill=false、unmatched_count、files[] 字段顺序固定，绝对 source_root 仅写入 manifest，review 只输出统计与 family 计数
- CLI 在 `not args.check and args.policy is None and args.profile_dir is not None` 时调用写入；旧 manifest / 00-source-inventory.md 存在则拒绝并以非零退出码结束，不覆盖；从不写 source-policy.json
- 新增 LocalCorpusOutputTests 两例：test_no_policy_writes_manifest_and_review 断言 manifest 字段、family 计数、SHA 前后一致、review 不含源正文、不写 source-policy；test_no_policy_with_existing_profile_is_rejected_without_overwrite 断言旧 manifest 内容不变
- 改动文件：skills/nuwa-skill/scripts/inventory_local_corpus.py、tests/test_nuwa_local_corpus.py、scripts/ralph/prd.json、scripts/ralph/progress.txt
- 验证：python3 -m unittest tests.test_nuwa_local_corpus -v（13 OK）；AST 解析；git diff --check 通过；CLI 端到端生成可被 json.tool 解析的 manifest
- **未来迭代的学习：**
  - macOS 上 /Volumes/WorkSSD/Cache/tmp 是 APFS firmlink，subprocess.run 调子进程创建的文件对父进程的 Path.is_file() 不可见；后续测试涉及写盘断言时优先 import 模块后直接调用函数，并把所有断言放在 finally 内的 rmtree 之前
  - manifest 是后续 source-policy 与 US-018 性能基线的输入契约：unmatched_count 与 files[].policy_class 字段名必须保持稳定；US-006 应用 first-match 后只能改 policy_class 字段值，不能新增或删除字段
  - 旧 manifest 存在时拒绝写入（mkdir(parents=True, exist_ok=False) + 文件存在检查）是幂等保护：US-007+ 的重跑应保留这条契约，由用户显式清理 profile 或换新 profile 目录
  - inventory review 的 00-source-inventory.md 是 Nuwa 写 source-policy 草稿的输入，因此 family 计数与 unmatched 摘要必须可被 rg/awk 解析；保持 markdown 列表格式 `- \`name\`: count` 不变

## 2026-07-25 11:20 - US-006
- 在 inventory_local_corpus.py 加入 POLICY_SCHEMA_VERSION/VALID_POLICY_CLASSES/DISTILLABLE_POLICY_CLASSES/SourcePolicyError；新增 load_source_policy（顶层对象、schema_version=1、rules 非空、class 合法、globs 非空字符串）和 apply_source_policy（first-match wins、生成新 records）；自定义 _glob_matches 支持 `**/` 与 `**` 跨目录
- CLI 在 --policy 时加载 policy，失败以退出码 2 + stderr 结束；policy 合法则 apply_source_policy 改写 records 的 policy_class，再走现有写盘路径
- write_manifest_and_review 接受 policy=None/loaded 两种调用；_build_manifest 在所有 eligible+非 excluded 文件进入 DISTILLABLE_POLICY_CLASSES 时设置 can_distill=true；excluded 不计入阅读预算
- inventory review 增加 `policy_class 计数` 段与「未匹配文件」相对路径清单；can_distill=true 时状态附文改为可进入六维研究
- 新增 SourcePolicyTests 7 例：first-match wins + unmatched 不阻塞、full policy → can_distill=true、invalid JSON/class/空 globs 拒绝、CLI 拒绝时不写盘且旧 manifest 不被覆盖、review 列出 class 计数与未匹配相对路径
- 修正 test_cli_accepts_policy_and_custom_max_file_bytes：原测试给一个空路径通过 --policy，US-006 引入真实加载逻辑后必须随附合法 policy fixture
- 改动文件：skills/nuwa-skill/scripts/inventory_local_corpus.py、tests/test_nuwa_local_corpus.py、scripts/ralph/prd.json、scripts/ralph/progress.txt
- 验证：python3 -m unittest tests.test_nuwa_local_corpus -v（20 OK）；SourcePolicyTests 7 例独立运行 OK；CLI 端到端生成可被 json.tool 解析的 manifest，can_distill=true；CLI 非法 JSON 退出码 2 且不创建 profile 目录；git diff --check 通过；AST 解析通过
- 提交：d113a15 feat: US-006 - 实现 source policy 校验和有序匹配
- 同步更新：scripts/ralph/prd.json 中 US-006 passes=true
- **未来迭代的学习：**
  - `first-match wins` + `excluded 不阻塞` 是 US-006 的核心契约：后续 US-007+ 在 manifest 中加 sha256/duplicate_group/version_group/semantic_read 字段时，复用同一份 records，不得绕过 apply_source_policy 直接写 policy_class
  - `load_source_policy` 在 CLI 入口处执行，校验失败时跳过整段写盘逻辑（包括 mkdir profile_dir），所以“CLI 拒绝时不创建 profile 目录”是隐性契约；US-009/quality_check.py 写新入口时不要把 profile 目录创建提前到 policy 校验之前
  - `DISTILLABLE_POLICY_CLASSES` 是 manifest 顶部 can_distill 的输入；US-013 文档/US-018 真实语料测试都应复用同一集合；如需加入第六类，必须先在 self-distill-workflow.md §2 同步更新枚举表
  - `_glob_matches` 用 regex 而非 fnmatch/PurePath.match，是为支持 `**/`；任何后续 policy 编辑工具或 GUI 必须复用同一定义，否则会出现「policy 写 `.md` 但 inventory 不命中」的 silent mismatch
  - inventory review 末尾 `## 未匹配文件` 的相对路径是 Nuwa 提 Checkpoint A policy 草稿的输入；当前实现每条都以 `- \`path\`` 列出，US-009 merge_research / quality_check 不要重新解析，只 rg `\`- 即可
  - US-006 的 test_cli_accepts_policy_and_custom_max_file_bytes 改成「提供合法 policy」后，未来如果再次收紧 policy 校验（如要求 rules 至少覆盖 N 个 family），同样要同步更新这个 smoke test，不能只补新测试

## 2026-07-25 11:36 - US-007
- 在 inventory_local_corpus.py 中新增 SHA-256 计算（_compute_sha256, 64KB chunked read）与 deterministic 重复分组（assign_duplicate_groups / count_duplicate_summary）；每条 record 新增 sha256 / duplicate_group / semantic_read 字段；按 relative_path 字典序最小者作 representative，其余 semantic_read=false；阅读失败抛 InventoryError，不静默跳过
- _build_manifest 把三个新字段写入 files[]；_write_inventory_review 新增「## 重复文件」段输出 duplicate_files / duplicate_groups 与语义说明；review 仍只统计与 family 摘要，不出现源文件正文
- 新增 DuplicateDetectionTests 7 例：精确重复共享 group + representative 字典序最小、sort 后稳定性、每条 record 都有 64 字符 SHA-256、chmod(0) 读取失败抛 InventoryError、review 输出 dup 统计且不含源文件正文、apply_source_policy 后 duplicate_group 保持、_compute_sha256 直读小文件
- 同步更新 LocalCorpusTraversalTests 与 LocalCorpusOutputTests 旧字段集合断言（加入 sha256 / duplicate_group / semantic_read 三个新键）
- 改动文件：skills/nuwa-skill/scripts/inventory_local_corpus.py、tests/test_nuwa_local_corpus.py、scripts/ralph/prd.json、scripts/ralph/progress.txt
- 验证：python3 -m unittest tests.test_nuwa_local_corpus -v（27 OK，包括旧 20 + 新 7）；AST 解析；git diff --check 通过；CLI 端到端在合成 3 文件（其中两份精确重复）上输出 manifest 含 sha256/duplicate_group/semantic_read 字段、review 含 duplicate_files=1, duplicate_groups=1、不含源文件正文
- 提交：9156e2d feat: US-007 - 增加 SHA-256 重复分组
- 同步更新：scripts/ralph/prd.json 中 US-007 passes=true
- **未来迭代的学习：**
  - record 字段集合是 manifest 契约：US-007 新增 sha256/duplicate_group/semantic_read 三个键必须与 US-006 八个键共存；US-008/后续 story 再加 version_group 时继续延展键集，不得删键或原地改类型
  - 重复分组以 sha256 字符串本身作为 duplicate_group 的稳定标识（first match wins 之外再加一次 SHA 哈希），下游 Nukwa quality check 与 reading budget 不需要再解析嵌套结构；如未来要切换为短 hash，必须同步更新 review 段
  - assign_duplicate_groups 直接修改传入的 records 列表，apply_source_policy 用 dict(record) 复制，因此 policy 改写后 duplicate_group 与 semantic_read 自动透传；US-008+ 接入版本序列时也保持「在原 records 上改字段」的模式，不要把 records 替换成新对象
  - chmod(0) 在 Unix 普通用户下能可靠触发 OSError；root 用户下需要换成 macOS chflags 隐藏或临时挪走来测试失败路径，US-008+ 不要再写「靠 root 拒绝」断言
  - profile-dir 必须在 source_root 之外：DuplicateDetectionTests 第一次失败是误把 profile 放在 source_dir 内部，触发 is_within 防护；后续 review / manifest 写入测试统一用 tempfile.mkdtemp 在 work_dir 下平级放置 source_root 与 profile_dir

## 2026-07-25 19:50 - US-008
- 在 inventory_local_corpus.py 中新增 `_VERSION_PATTERN`（大小写不敏感 `v\d+(?:\.\d+)*`）、`_strip_version`（最后一次匹配，去版本基础名 + tuple）、`_version_group_key`（parent_dir + base + ext 三元组）；新增 `assign_version_groups` 与 `count_version_summary`；每条 record 新增 `version_group` / `current_version` / `evolution_only` 三字段；sorted 用「数字 tuple + relative_path」复合 key，确保 V10 > V2；group_id 形如 `{parent_dir}/{base}v{highest}{ext}`，ineligible 文件不分组，无版本标记文件 version_group=None
- `_build_manifest` 把三个新字段写入 manifest files[]；`_write_inventory_review` 在「重复文件」与「未匹配文件」之间新增「## 版本序列」段，列出 `version_groups` 计数 + `current_version` 相对路径清单；review 仍不出现源文件正文
- 新增 VersionSelectionTests 5 例：v0.1/v0.2/v1 当前版本、case-insensitive 与数字 tuple 比较（V10 > V2）、无版本标记文件不参与分组、`assign_version_groups` 幂等、review 列出 version_groups 与 current 路径
- 同步更新 LocalCorpusTraversalTests 与 LocalCorpusOutputTests 旧 record/manifest 字段集合断言（加入 version_group / current_version / evolution_only 三个新键）
- 改动文件：skills/nuwa-skill/scripts/inventory_local_corpus.py、tests/test_nuwa_local_corpus.py、scripts/ralph/prd.json、scripts/ralph/progress.txt
- 验证：python3 -m unittest tests.test_nuwa_local_corpus -v（32 OK，包括旧 27 + 新 5）；AST 解析通过；git diff --check 通过；CLI 端到端合成 5 文件（v0.1/v0.2/V10 + unversioned + external），manifest 中 V10 current_version=true，三个版本同 group，review 显示 version_groups: 1 与 `notes/plan-V10.md`
- 提交：2cedc85 feat: US-008 - 增加版本序列 current 判定
- 同步更新：scripts/ralph/prd.json 中 US-008 passes=true
- **未来迭代的学习：**
  - `re.IGNORECASE` + `\d+(?:\.\d+)*` 已经能正确捕获 `v0.1`/`V10`/`v0.2.3`；后续若要支持字母数字版本（如 `v1a`），需要扩展 pattern 同时调整 tuple 比较语义，不要默默把字符串混进数字 tuple
  - `_strip_version` 用 `finditer` 的最后一个 match，比 `re.sub` 更稳，避免把同一 stem 中多个匹配全替换导致基础名漂移；后续任何「最后一次匹配」逻辑都遵循该模式
  - `assign_version_groups` 必须复用 `inventory_local_corpus` 已 sorted 的 records；不要在内部再 `records.sort(...)`，否则与 duplicate_group 的 representative 选取顺序耦合，会引入非确定行为
  - 「数字 tuple 比较」必须经 `_strip_version` 二次拆解（不要直接 `_VERSION_PATTERN.fullmatch(stem)`，因为 fullmatch 只接受整个 token）；在 lambda key 中调用 `_strip_version` 比单独写 `parse_version_token` 更不易踩坑
  - review 输出遵循已有 markdown 段顺序（摘要 → family 计数 → class 计数 → 重复 → 版本序列 → 未匹配），后续如果新增「外部归档」或「owner family」段，必须插在版本序列之后、未匹配之前，避免 Nuwa 后续 story 的 rg 索引偏移
  - version_group 的字符串形式含 parent_dir；root-level 文件（如 `notes-v1.md`）会被归入空 parent_dir，下游 story 若需 root-level 友好分组，可考虑把 parent_dir 为空时统一填 `.`，但本里程碑先保持原样，避免与 `top_level_family = "."` 约定冲突

## 2026-07-25 19:55 - US-009
- 重构 skills/nuwa-skill/scripts/merge_research.py 为 argparse + 双模式（person/self）；person 模式行为与字段集合保持不变；self 模式使用六个自我维度标签：positioning / core-theses / decisions-and-behavior / systems-and-cases / expression-dna / tensions-and-evolution
- self 模式从研究文件 frontmatter 的 sources / source_paths / relative_sources 字段读相对源路径；正文 fallback 仅匹配裸词相对路径；维度内与跨维度双层去重（同一相对路径只计一次）；不再依赖 URL
- self 模式自动加载 references/source-manifest.json，按 policy_class 聚合来源 class 计数；缺失 manifest 时降级到仅去重统计，提示写到 stderr
- _detect_mode 检查 SKILL.md 的 `profile_type: self` frontmatter 自动选 self；显式 --mode person/self 始终覆盖自动检测
- 新增 tests/fixtures/nuwa-self-distill/merge_research/{person,self} 与两份 golden_output.txt；MergeResearchCompatibilityTests 7 例全部通过
- 改动文件：skills/nuwa-skill/scripts/merge_research.py、tests/test_nuwa_local_corpus.py、tests/fixtures/nuwa-self-distill/merge_research/{person,self}*、scripts/ralph/prd.json、scripts/ralph/progress.txt
- 验证：python3 -m unittest tests.test_nuwa_local_corpus -v（39 OK，含旧 32 + 新 7）；AST 解析；git diff --check 通过；person fixture golden 在含/不含 --mode person 下输出逐字节匹配
- 提交：9b71eb6 feat: US-009 - 扩展 merge_research.py 支持 self 模式
- 同步更新：scripts/ralph/prd.json 中 US-009 passes=true
- **未来迭代的学习：**
  - merge_research.py 的 CLI 接口是 Nuwa 自检的固定入口：US-013/US-017 后续若要替换表格样式，必须保持「六维度 + 总来源 + 矛盾 + 信息不足」四行结构，便于 nuwa 脚本/文档 rg 索引
  - self 模式 class 计数放在表格外（避免 24 字符槽位截断），约定形式为 `class counts: name=count`，按 count 降序名字典序排；后续如果 US-013 资产卡审计要复用，必须复用同一字符串形态
  - frontmatter 解析只用极简手写 YAML（不支持嵌套、注释块、tag），目的是避免 PyYAML 依赖；US-010 quality_check.py 若也需要 frontmatter，建议复用同一解析函数而非各写一套
  - self 模式路径 fallback 正则 `(?<![\w/])((?:[A-Za-z0-9_\-]+/)*[A-Za-z0-9_\-]+\.[A-Za-z0-9]{1,8})(?![\w/])` 会把 URL scheme 与绝对路径过滤掉，但**仍会把 markdown 链接中的相对路径误判为来源**；US-010 隐私扫描若要更严格，须先排除 markdown link 形式
  - _detect_mode 只识别 `profile_type: self`，后续 US-012/US-013 如果扩展 self 变体（如 `profile_type: self-redacted`），需要在 _detect_mode 内补 mapping，且默认 self 仍走 SELF_AGENTS 六标签
  - 显式 `--mode person` 在 profile_type=self 的 profile 下应继续可用（如某人想用公众人物工具审计自我 profile），不要在 _detect_mode 里禁止显式覆盖

## 2026-07-25 20:00 - US-010
- 重构 skills/nuwa-skill/scripts/quality_check.py：增加 `--mode {self,person}` 与 profile_type frontmatter 自动识别；self 模式跑 4 项硬性检查（七章节齐全 / assets/index.md 存在 / SKILL.md 含 assets/ 相对链接 / token 预算 3000–6000），person 模式沿用旧六项心智模型检查并保持退出码契约
- 新增 check_self_sections / check_assets_index / check_assets_link / check_self_token_budget / estimate_tokens / detect_profile_type；token 估算使用 `CJK 字符数 + ceil(非 CJK 字符数 / 4)`，>6000 退出非零、<3000 仅警告
- 测试：tests/test_nuwa_local_corpus.py 新增 QualityStructureTests 10 例（干净 self 通过、缺失章节列出全部缺失名、缺 assets/index.md 失败、缺 assets/ 链接失败、token>6000 失败、token<3000 仅警告、SKILL.md 路径作为 target、显式 --mode 覆盖 frontmatter 自动检测、person 旧契约回归、自动检测 self）
- 改动文件：skills/nuwa-skill/scripts/quality_check.py、tests/test_nuwa_local_corpus.py、scripts/ralph/prd.json、scripts/ralph/progress.txt
- 验证：python3 -m unittest discover -s tests -p 'test_nuwa_*.py' -v（49 OK，含旧 39 + 新 10）；AST 解析；git diff --check 通过
- 同步更新：scripts/ralph/prd.json 中 US-010 passes=true
- **未来迭代的学习：**
  - `quality_check.py` 接受 profile 目录或 SKILL.md 路径；当 target 是文件时把 parent 视作 profile 目录（用于找 assets/index.md）。后续 US-011/US-019 闭环测试可复用同一入口；不要再拆出 `--profile-dir` 之类参数
  - 自动检测只看 SKILL.md frontmatter 的 `profile_type:`；profile 目录附带 references/source-manifest.json 不影响模式识别。若 US-019 闭环需要 audit 一个「public SKILL.md 但来自 self 流程」的 profile，仍以 frontmatter 为准
  - token 估算把 frontmatter + 章节标题 + 链接文本都算上；self 模板第 1 行 frontmatter + 7 章节标题 + 7 个 `[..](../assets/x.md)` 链接大约消耗 ~200 tokens，写 US-019 闭环时如要让 SKILL.md 落在 3000–6000，正文 CJK 字符数应控制在 3000 左右
  - person 模式保留旧退出码契约（total-1 通过仍 exit 0），是因为旧调用方可能依赖它；US-019 全量回归前不要再收紧，避免把 person 流程也变成非零
  - privacy 扫描规则（CREDENTIAL_ASSIGNMENT/WECHAT_ID/CHATROOM_ID/PRIVATE_IPV4）按 self-distill-workflow.md §8 由 US-011 实现，本 milestone 仅占位并暴露 `check_assets_*` 系列函数；US-011 接入时复用 `resolve_profile_paths` + `check_assets_index`，不要重新走文件系统

## 2026-07-25 20:30 - US-011
- 在 skills/nuwa-skill/scripts/quality_check.py 增加 PRIVACY_RULES（4 个稳定 rule id）、scan_privacy_in_text、scan_privacy_gate、format_privacy_findings，导入 ipaddress 做 RFC1918 判定；self 模式契约加入「公开面隐私扫描」硬性检查，输出 5 项 5/5
- 隐私扫描只走 SKILL.md + assets/*.md，references/source-manifest.json 与 references/research/ 完全跳过；findings 只输出 rule id + 相对路径 + 行号，敏感值永不回显
- 测试新增 PrivacyGateTests 8 例：干净 profile 通过、wxid_/chatroom/credential/RFC1918 IPv4 各命中、public IPv4 不误报、references/ 即使含 wxid_/chatlog/微信/10.0.0.7 也不触发、findings 行号格式；更新 QualityStructureTests.test_self_clean_profile_passes 的 4/4 → 5/5 断言
- 改动文件：skills/nuwa-skill/scripts/quality_check.py、tests/test_nuwa_local_corpus.py、scripts/ralph/prd.json
- 验证：python3 -m unittest discover -s tests -p 'test_nuwa_*.py' -v（57 OK，含旧 49 + 新 8）；AST 解析通过；git diff --check 通过；CLI 端到端在临时 profile 上命中 token=fake_should_not_match 时输出 `CREDENTIAL_ASSIGNMENT @ SKILL.md:10` 但不回显值
- 提交：091f0bb feat: US-011 - 增加 self profile 公开面隐私扫描
- 同步更新：scripts/ralph/prd.json 中 US-011 passes=true
- **未来迭代的学习：**
  - `PRIVACY_RULES` 是 self-distill-workflow.md §8 锁定的稳定 rule id + pattern 集合；US-013/US-019 若扩展规则（如 EMAIL_ADDRESS / PHONE_NUMBER），必须复用同一 dict 结构、保持 id 不变；新增规则时优先在 references/self-distill-workflow.md §8 同步更新枚举表
  - 隐私扫描的输出策略是「绝不回显敏感值」：findings 只打印 rule id / 相对路径 / 行号。下游任何日志/UI/脚本必须复用该格式，否则会破坏 US-019 闭环断言
  - PRIVATE_IPV4 用 ipaddress.IPv4Address 校验 `_is_private_ipv4` 才能正确区分公网/内网（避免 8.8.8.8 误报）；纯 regex 会把公网 IP 误报为内网，后续若引入 IPv6 规则必须同样走 ipaddress.IPv6Address，不要简单扩展 regex
  - scan_privacy_gate 只 glob `assets/*.md`，不递归子目录；这是与 quality_check 同级调用约定一致的简化策略，US-019 全量回归时若 `assets/` 出现子目录需要先在此扩 glob
  - 隐私扫描只走公开面（SKILL.md + assets/*.md），references/ 完全跳过；US-017 合成语料端到端测试在写「私有面命中」断言时必须严格把 fixture 放到 references/ 下，否则会变成 US-011 误报
